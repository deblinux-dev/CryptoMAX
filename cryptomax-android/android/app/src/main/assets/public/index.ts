/**
 * CryptoMAX — Capacitor entry point
 *
 * The main WebView loads web.max.ru via server.url in capacitor.config.ts.
 * CryptoBridgePlugin (native) injects engine-bundle.js, preload-main.js,
 * and ui-panel.js into the WebView on each page load.
 *
 * This file is only used for the fallback page (src/index.html).
 */

// No initialization needed here — all logic is in injected scripts.
export {};
