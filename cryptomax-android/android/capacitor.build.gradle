// capacitor.build.gradle — auto-generated by `npx cap sync`
// Defines SDK versions + applicationId used by the rest of the gradle build.
//
// This file is checked-in so the project builds even before the first
// `npx cap sync` is run.

def ext = gradle.ext
ext.minSdkVersion = 24
ext.compileSdkVersion = 36
ext.targetSdkVersion = 36
ext.applicationId = "com.cryptomax.app"
